#this is the workflow name

name: hello workflow
on:
  push:
    branches: [main]
jobs:
  greet:
  #each jobs runs on a runner
    runs-on: ubuntu-latest  
    steps:
      - name: say hello to users
        run: echo "Hello dosto"

      - name: say  hello to username
        run: echo "Hello $USER"
